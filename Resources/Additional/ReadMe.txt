To have an answer file copied to the image, it MUST be named 'unattend.xml'
The unattend.xml is copied to the '\Windows\Panther' directory where it will automatically run during Windows 10 installation.

Scripts and files are copied to the '\Windows\Setup\Scripts' directory.